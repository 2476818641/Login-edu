# 抓包流程（校园网网页认证）

## 什么时候需要抓包

本仓库的认证脚本已经按一次真实抓包实现好了（三步 API + AES 加密的 `pass` 字段），**正常情况下不用抓包**。
只有这两种情况需要自己抓：

1. **你的门户和它不一样**（接口路径 / 字段名 / 密码处理方式不同）→ 抓一份，按 `AI-PROMPT.md` 交给 AI 出结论；
2. **想补漏**（例如失败样本、心跳保活请求）。

> ⚠️ Burp XML 里含**明文密码、学号、会话 cookie**。不要提交进 git（仓库 `.gitignore` 已挡住 `*.xml`、`小抄*.md`），
> 贴给外部 AI 前先跑 `burp-xml-summary.py` 脱敏。

## 一、放在哪里

| 位置 | 说明 |
|---|---|
| 本仓库根目录 | 克隆下来时直接丢进去就行，`.gitignore` 会挡住，不会推上去 |
| `/tmp/` 下任意目录 | 临时放一下、或不想放在仓库里时用 |

命名建议：`<学校简称>-<日期>-<有线|无线>.xml`，例如 `xxu-20260920-wired.xml`。

## 二、抓包要求（决定成败的就 3 条）

1. **从"还没认证"的状态开始抓**：先断线（拔网线 / 断开 Wi-Fi / 路由器上 `ifdown wan`），再开 Burp，再插上/连上。
2. **故意输错一次密码，再输对一次** —— 有成功/失败对照，判定条件才可靠；只有成功样本时只能靠"外网通不通"兜底，容易误判。
3. **抓全一次"页面加载"**：认证页打开时按 **`Ctrl+F5` 强制刷新**（绕过浏览器缓存，JS 才会重新下载）——
   密码怎么算的通常就藏在这批 JS 里（本项目正是靠这一步拿到 AES 密钥与 `encode()` 函数）。
   认证完成、外网通了就停 Burp；刷论坛/看视频的流量会把 XML 撑到几十 MB，还会把认证条目淹掉。

## 三、怎么抓

**方案 A（推荐）：电脑接在路由器 LAN 口，用 Burp 抓**

1. 电脑网线插路由器 **LAN 口**（拿到 192.168.1.x），确认能打开认证页（此时外网还不通，正常）
2. Burp → Proxy → Options → 监听 `0.0.0.0:8080`
3. 浏览器/系统代理设为 `127.0.0.1:8080`，装好 Burp 证书（抓 https 必需）
4. 打开认证页 → `Ctrl+F5` 强刷 → 故意输错密码登录一次 → 再输对一次
5. Burp → HTTP history → 看有没有内网地址的 `POST` 和 `302 + Location`

**方案 B：浏览器 F12**

F12 → Network → 勾 **Preserve log** → `Ctrl+F5` → 登录 → 找 `POST`（或 XHR/fetch）那条 →
右键 `Copy as cURL`，或看 `Payload` / `Response` 标签。

## 四、导出与自检

Burp → **Proxy → HTTP history** → 全选（`Ctrl+A`）→ 右键 → **Save items** → 存成 `.xml`。

导出后自检三条：

- 有**一条 POST**、目标是内网地址（`10.x` / `172.16~31.x` / `192.168.x` / `100.64~127.x`）？
- 有 **302 + Location** 指向认证页？→ 门户是"劫持重定向"型，很常见
- 那条 POST 的**成功与失败响应**是否不同？两条原文都留着

## 五、要交出来的内容

### ① 完整登录请求（必给）

```
POST /api/login.php HTTP/1.1                     ← 请求行（方法 + 路径 + query）
Host: 10.30.100.5                                ← 认证接口地址与端口
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
User-Agent: Mozilla/5.0 (...)
Referer: http://10.30.100.5/
Cookie: RAASSESSID=xxxxx                         ← 有 cookie 就是关键信息
                                                 ← 空行以下是 body
user=2026xxxx&pass=xxxx&authmode=0&pool=&isp_id=0&pxyacct=
```

**尤其注意 body 里有没有这些**：`mac` / `ip` / `nasip` / `wlanacname` / `wlanuserip` / `t`（时间戳）/
`sign` / `token` / `callback`（jsonp）。有就原样保留。

### ② 完整响应（必给）

```
HTTP/1.1 200 OK
Content-Type: application/json

{"ret":0,"data":[],"msg":"认证成功！"}            ← 成功时长什么样
```

### ③ 失败一次的响应（强烈建议）

故意输错密码那条。有成功/失败对照，判定条件才可靠。

### ④ 其它（有就给）

| 项 | 为什么需要 |
|---|---|
| 登录前是否先 `GET` / 先打别的接口（如 `ip.php`） | 有些门户要先拿 session/cookie 或登记客户端 IP |
| `pass=` 后面是不是明文 | 若是 32 位 hex，说明前端做了哈希或加密 → 需要页面里那段 JS |
| **认证页的 JS**（`Ctrl+F5` 后抓包，或直接看源码） | 密码处理逻辑通常就在里面（本项目就是从这里挖出 AES 密钥的） |
| 登录成功后 5~10 分钟内的周期性请求 | 有的校园网要心跳保活，否则被踢下线 |
| 登出接口 / 认证页 HTML | 可选，便于切换账号与确认表单字段 |

## 六、先脱敏，再贴给 AI

```sh
python3 burp-xml-summary.py 你的抓包.xml -o 小抄.md
# --all 不筛选全留 / --no-redact 不脱敏 / --max-body N 调整单条长度
```

输出 5 节：POST 目标及字段名 / 跳转 Location / 成功失败字样 / 认证页表单字段 / 逐条明细。
脱敏规则：请求体里的密码与令牌值 → `<已脱敏 N 字符>`，账号 → `<账号 N 位数字>`，cookie 只留名字；
**响应体保留原文**（成功/失败判定串必须逐字保留，改了就写不成 `case` 模式）。

把小抄 + `AI-PROMPT.md` 的提示词一起贴给任意 AI，或直接把小抄/XML 发我。

## 七、抓不到想要的东西怎么办

- **认证页打不开**：确认电脑插在路由器的 **LAN 口**（lan1~lan3）；WAN 口是给校园网网线用的，此时外网不通是正常的。
- **没有 302 劫持**：手动打开认证页（问同学，或看 DHCP 下发的提示页地址）。
- **页面是 HTTPS 且解不开**：在客户端装 Burp 证书（Proxy → Options → 导入/导出 CA 证书 → 装到"受信任的根"）。
- **看不到 JS**：多半是浏览器缓存 —— 用 `Ctrl+F5` 强刷，或在 F12 → Network 里勾 Disable cache 再刷新。
- **学校用 802.1X（WPA-Enterprise）而不是网页认证**：不在本仓库脚本范围内，抓包也抓不到 HTTP 登录。
